# Vite "production" mode (default for `npm run build` / `npm run tauri build`).
# Builds the installer that points at the PROD Railway backend.
# NOTE: prod Convex (unique-jaguar-230) must be provisioned first (see plan D1.5).
VITE_BACKEND_WS_URL=wss://api.wininterview.xyz
VITE_CONVEX_URL=https://unique-jaguar-230.convex.cloud
