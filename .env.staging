# Vite "staging" mode → build an installer that points at the DEV Railway backend.
# Build with:  npm run build -- --mode staging   (or tauri build with a staging beforeBuildCommand)
VITE_BACKEND_WS_URL=wss://api-dev.wininterview.xyz
VITE_CONVEX_URL=https://qualified-cuttlefish-550.convex.cloud
