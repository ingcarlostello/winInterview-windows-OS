Listo. Resumen de lo entregado y lo que falta (en tu repo de backend).

✅ Hecho en reactjs-site (frontend, sin tocar el backend)
Dependencias: convex, @clerk/clerk-react, lucide-react.
Providers en main.tsx: ClerkProvider + ConvexProviderWithClerk.
Rutas en App.tsx: /sign-in, /sign-up, /dashboard (+ enlace en Header.tsx con SignedIn/SignedOut).
Páginas/componentes: SignInPage, SignUpPage, DashboardPage, UserKeyCard, EnsureConvexUser, tema Clerk en clerkAppearance.ts — todo con tus tokens (Glass, Button, lima/dark).
Conexión al backend ya desplegado vía src/lib/convexApi.ts con makeFunctionReference (no genera convex/_generated, así no hay riesgo de pisar el backend). Llama a tus funciones reales: users:storeUser, users:getCurrentUser, y users:regenerateUserKey (esta última la añades tú).
Entornos: .env.local (dev) y .env.production (prod) con las keys públicas de Convex/Clerk de cada entorno.
Verificado: tsc --noEmit ✅, eslint ✅, dev server sirve / y /dashboard (HTTP 200).
Eliminé los convex/schema.ts|users.ts|auth.config.ts|lib/userKey.ts que había creado (habrían borrado tu backend al pushear). convex/ quedó como estaba.
El dashboard ya funciona hoy mostrando email + plan. La userKey aparece (con copiar/regenerar) en cuanto el backend la exponga.

⚠️ Importante
No ejecutes npx convex dev ni convex deploy desde este repo — su convex/ no tiene tus funciones y las borraría. El backend se gestiona desde tu otro repo.
Dejé APP_ENV=test en el Convex dev (por si tu generador lo usa para el prefijo). Si no lo quieres, lo quito.
📋 Lo que debes añadir en tu repo de backend de Convex (para la userKey)
1. Esquema — en tu users (convex/schema.ts):


// dentro de defineTable({...}) de users:
userKey: v.optional(v.string()),
// ...y un índice:
.index('by_user_key', ['userKey'])
2. Generador (convex/lib/userKey.ts):


const ALPHABET = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789';
export function generateUserKey(): string {
  const env = process.env.APP_ENV ?? 'test'; // 'test' en dev, 'live' en prod
  const bytes = new Uint8Array(40);
  crypto.getRandomValues(bytes);
  let s = '';
  for (let i = 0; i < bytes.length; i++) s += ALPHABET[bytes[i] % ALPHABET.length];
  return `wik_${env}_${s}`;
}
3. Asignarla al crear el usuario — en createUserFromClerk (y/o storeUser), al insertar:


import { generateUserKey } from './lib/userKey';
// en el insert del usuario nuevo, añade:
userKey: generateUserKey(),
4. getCurrentUser — si tiene returns validator, añade userKey: v.optional(v.string()). Si devuelve el doc tal cual, ya lo incluye.

5. Mutación nueva regenerateUserKey (pública, la llama el dashboard):


export const regenerateUserKey = mutation({
  args: {},
  handler: async (ctx) => {
    const identity = await ctx.auth.getUserIdentity();
    if (!identity) throw new Error('No autenticado');
    const user = await ctx.db.query('users')
      .withIndex('by_clerk_id', (q) => q.eq('clerkId', identity.subject)).unique();
    if (!user) throw new Error('Usuario no encontrado');
    const userKey = generateUserKey();
    await ctx.db.patch(user._id, { userKey });
    return userKey;
  },
});
6. Backfill de usuarios existentes (una vez, internal mutation o desde el dashboard de Convex): recorrer users sin userKey y asignarles uno.

7. (Para la app Tauri) una internalQuery getUserByKey sobre by_user_key que tu backend Railway pueda invocar para validar la key — equivalente a lo que hoy hace el intercambio de token de Clerk en desktop-auth.html.

Cuando lo despliegues, configura también las env vars de Convex prod (unique-jaguar-230) que hoy están vacías: CLERK_ISSUER_URL=https://clerk.wininterview.xyz, CLERK_SECRET_KEY=sk_live_…, APP_ENV=live, y las de Paddle.

¿Quieres que deje la userKey opcional así (mostrada cuando el backend la habilite), o prefieres que prepare un PR/parche concreto si me das acceso/ruta al repo del backend?