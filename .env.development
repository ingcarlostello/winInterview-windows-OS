# Vite "development" mode (npm run dev / npm run tauri dev).
# The Python backend runs locally on :8000. VITE_CONVEX_URL comes from
# .env.local (gitignored) on the dev machine, so it's not duplicated here.
VITE_BACKEND_WS_URL=ws://localhost:8000
