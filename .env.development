# Vite "development" mode (npm run dev / npm run tauri dev).
# The Python backend runs locally on :8000. VITE_CONVEX_URL / VITE_PADDLE_CLIENT_TOKEN
# come from .env.local (gitignored) on the dev machine, so they're not duplicated here.
VITE_BACKEND_WS_URL=ws://localhost:8000
